{
  "typescript.tsdk": "node_modules/typescript/lib", // Specify the TypeScript SDK path
  "typescript.validate.enable": true,
  "files.exclude": { // Exclude node_modules from the file explorer to reduce clutter
    "**/node_modules": true
  },
  "search.exclude": { // Exclude node_modules from search results
    "**/node_modules": true
  }
}
